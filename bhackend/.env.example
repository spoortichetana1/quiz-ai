# Copy this file to ".env" (same folder) and fill values.
# Do NOT upload .env to GitHub.

OPENAI_API_KEY=your_key_here # full key full


# If you don't have a key, set this true to always use mock generation
QUIZ_USE_MOCK=true

# Model name (works if your key has access)
OPENAI_MODEL=gpt-4o
